# string_to_palindrome_via_all_permutations
Given a string, write a function to determine if a permutation of the string could form a palindrome.  #For example, #"code" -> False, "aab" -> True, "carerac" -> True.
